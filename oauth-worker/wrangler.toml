name = "prepfusion-decap-oauth"
main = "worker.js"
compatibility_date = "2024-01-01"

# GITHUB_CLIENT_ID and GITHUB_CLIENT_SECRET are set as secrets, not here —
# see ../admin/OAUTH_SETUP.md. Never commit real secret values to this file.
